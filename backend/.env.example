# Copy this file to .env (already gitignored) and fill in your own key.
# Never commit a real key - only this placeholder file is tracked.

# Required to use the AI scheduling agent (Phase 9). Not required to run
# the rest of the application or the test suite.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini if unset.
OPENAI_MODEL=
